\programVariables {
 
  int divident, divisor;
  int result;
}

\problem {
 (divident >= 0 & divisor >= 0) ->
    \<{
        if ( divisor != 0 ) {
           result = divident / divisor;
       } else {
           result = -1;
       }
  }\> (divisor = 0 <-> result = -1) 

}